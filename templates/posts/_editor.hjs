<div class="editor">
  <form action="{{urls.createComment}}" method="post">
    <input type="hidden" name="_csrf" value="{{csrfToken}}" />
    <textarea name="comment[content]"></textarea>
    <button class="btn" type="submit">Comment</button>
  </form>
</div>
